query ProductsGetList($first: Int, $skip: Int) {
    productsConnection(first: $first, skip: $skip) {
        products: edges {
            node {
                ...ProductListItem
            }
        }
        pageInfo {
            hasNextPage
            hasPreviousPage
        }
        aggregate {
            count
        }
    }
}