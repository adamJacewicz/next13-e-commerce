query ProductsGetByCollectionSlug($slug: String!, $skip: Int, $first: Int) {
    productsConnection(first: $first, skip: $skip, where: { collections_some: { slug: $slug } }) {
        products: edges {
            node {
                price
                name
                description
                id
                images {
                    url
                }
                categories {
                    name
                    id
                }
            }
        }
        pageInfo {
            hasNextPage
            hasPreviousPage
        }
        aggregate {
            count
        }
    }
}
